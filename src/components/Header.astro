---
import { navItems } from "../data/site";
import { whatsappUrl } from "../lib/contact";

const currentPath = Astro.url.pathname;

const rubricItems = [
  { label: "Administrasi", href: "/rubrik/administrasi" },
  { label: "Opini", href: "/rubrik/opini" },
  { label: "Konten Pramuka", href: "/rubrik/konten-pramuka" },
  { label: "Pembinaan", href: "/rubrik/pembinaan" },
  { label: "Kritik", href: "/rubrik/kritik" },
];

const isActive = (href: string) => {
  if (href === "/") return currentPath === "/";
  return currentPath === href || currentPath.startsWith(`${href}/`);
};

const isEdukasiActive = currentPath.startsWith("/artikel") || currentPath.startsWith("/rubrik") || currentPath === "/about";
const isResourceActive = currentPath.startsWith("/produk") || currentPath.startsWith("/kelas") || currentPath === "/rubrik/administrasi";
---

<header class="topbar">
  <div class="shell topbar-inner">
    <a class="brand" href="/" aria-label="PramukaUpdate">
      <span class="brand-mark" aria-hidden="true">PU</span>
      <span class="brand-copy">
        <span class="brand-name"><strong>Pramuka</strong><em>Update</em></span>
        <span class="brand-tagline">Media Edukasi & Informasi Pramuka</span>
      </span>
    </a>

    <button class="menu-toggle" type="button" aria-expanded="false" aria-controls="site-menu">
      <span>Menu</span>
      <i aria-hidden="true"></i>
    </button>

    <nav id="site-menu" class="primary-nav" aria-label="Navigasi utama">
      <a class={isActive("/") ? "active" : ""} href="/">Beranda</a>
      
      <details class="nav-dropdown" open={isEdukasiActive}>
        <summary class={isEdukasiActive ? "active" : ""}>Edukasi & Artikel</summary>
        <div class="nav-dropdown-panel mega-menu-panel">
          <div class="mega-menu-col">
            <strong>Rubrik</strong>
            <a href="/rubrik/opini">Opini</a>
            <a href="/rubrik/kritik">Kritik</a>
            <a href="/rubrik/pembinaan">Pembinaan</a>
          </div>
          <div class="mega-menu-col">
            <strong>Jelajahi</strong>
            <a href="/artikel">Semua Artikel</a>
            <a href="/about">Tentang Kami</a>
          </div>
        </div>
      </details>

      <details class="nav-dropdown" open={isResourceActive}>
        <summary class={isResourceActive ? "active" : ""}>Resource & Tools</summary>
        <div class="nav-dropdown-panel mega-menu-panel">
          <div class="mega-menu-col">
            <strong>Download</strong>
            <a href="/produk">Produk Digital</a>
            <a href="/rubrik/administrasi">Administrasi</a>
          </div>
          <div class="mega-menu-col">
            <strong>Belajar</strong>
            <a href="/kelas">Kelas Pembinaan</a>
          </div>
        </div>
      </details>
    </nav>

    <div class="header-actions">
      <a class="header-search" href="/artikel#pagefind-search">Cari</a>
      <a class="button button-solid header-wa" href={whatsappUrl("Halo PramukaUpdate, saya ingin bertanya.")}>WhatsApp</a>
    </div>
  </div>
</header>

<script>
  const topbar = document.querySelector(".topbar");
  const menuToggle = document.querySelector(".menu-toggle");
  const siteMenu = document.querySelector("#site-menu");

  menuToggle?.addEventListener("click", () => {
    const isOpen = topbar?.classList.toggle("is-open") ?? false;
    menuToggle.setAttribute("aria-expanded", String(isOpen));
  });

  siteMenu?.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", () => {
      topbar?.classList.remove("is-open");
      menuToggle?.setAttribute("aria-expanded", "false");
    });
  });
</script>
